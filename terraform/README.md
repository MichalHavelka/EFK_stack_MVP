# Notes
using env variables for secret and access keys:
export AWS_ACCESS_KEY_ID=""
export AWS_SECRET_ACCESS_KEY=""

Basic terraform that deploys 2 ec2 instances with vpc, subnet, igw etc. #TODO: if there is time, prolly make the ec2_instance module as some kind of loop so you can just select number of instnaces and increment number in name
